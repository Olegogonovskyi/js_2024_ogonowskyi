import React, {FC} from 'react';
import UserJsonDetailComponent from "../Components/JsonComponents/UserJsonDetailComponent/UserJsonDetailComponent";

const UserJsonDetailPage: FC = () => {
    return (
        <div>
            <UserJsonDetailComponent/>
        </div>
    );
};

export default UserJsonDetailPage;